{
  "name": "hhplus-ecommerce-mock-server",
  "version": "1.0.0",
  "description": "HH Plus E-Commerce Mock API Server for Stock and Coupon Management",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "start:delay": "json-server --watch db.json --port 3000 --delay 1000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}